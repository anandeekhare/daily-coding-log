Ship Packages Within D Days

Search space:
capacity from max(weights) to sum(weights)

Check:
How many days needed for given capacity

If days <= limit → valid → try smaller
Else → increase capacity

Pattern: Binary Search on Answer
